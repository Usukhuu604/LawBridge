overwrite: true
schema:
  - "https://lawbridge-c1kt.onrender.com/graphql"
  # - "http://localhost:4000/graphql"

documents:
  - "./src/**/*.graphql"

generates:
  ./src/generated/index.ts:
    config:
      scalars:
      Date: string
      reactApolloVersion: 3
      withHooks: true
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
